# API Configuration
VITE_API_URL=http://127.0.0.1:8000/api/v1/

# WebSocket Configuration
# Note: Set to 'true' if Django backend doesn't have Django Channels configured
# When disabled, notifications will use HTTP polling fallback (30s interval)
VITE_WS_URL=ws://127.0.0.1:8000/ws/notifications/
# VITE_DISABLE_WEBSOCKET=true
